package app.data.entity;

/**
 * Daftar kategori buku yang tersedia di sistem.
 * Berada di paket app.data.entity yang diekspor secara publik
 * oleh modul app.data, sehingga boleh dipakai modul lain.
 */
public enum Kategori {
    FIKSI,
    NON_FIKSI,
    TEKNOLOGI,
    ANAK,
    KOMIK
}
